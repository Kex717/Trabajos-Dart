void main() {
  double taller1 = 4.0; // Ejemplo: nota del primer taller
  double taller2 = 3.5; // Ejemplo: nota del segundo taller
  double examenFinal = 3.8; // Ejemplo: nota del examen final
  
  double notaDefinitiva = (taller1 * 0.3) + (taller2 * 0.3) + (examenFinal * 0.4);
  
  print("La nota definitiva del alumno es: $notaDefinitiva");
}